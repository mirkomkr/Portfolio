---
import { Image } from 'astro:assets';
const {title, description, image, altimage, link} = Astro.props;
---

<li class="focus-within:ring-2 focus-within:ring-accent relative group rounded-2xl flex flex-col transition-all duration-500 overflow-hidden hover:bg-primary/[0.03] hover:-translate-y-1">
    
    <Image
      src={image} 
      class="transition-transform duration-700 group-hover:scale-105 aspect-video object-cover" 
      alt={altimage} 
    />
    
    <div class="text-primary p-6">
      <h3 class="group-hover:text-accent transition-colors duration-300 text-2xl font-bold mb-2 font-title">
        {title}
      </h3>
      <p class="leading-relaxed text-sm opacity-70">
        {description}
      </p>
    </div>

    <a href={link} 
       class="after:content-[''] after:absolute after:inset-0 focus:outline-none" 
       target="_blank" 
       rel="noopener noreferrer" 
       aria-label={`Scopri di più su ${title}`}>
    </a>
</li>
